<%= simple_form_for @book, url: author_books_path(@author) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.input :name %>
  </div>

  <div class="field">
    <%= form.input :published_date %>
  </div>

  <div class="field">
    <%= form.input :description %>
  </div>
  
  <div class="field">
    <%= form.input :page_number %>
  </div>

  <div class="actions">
    <div class="row">
      <div class="col-xs-3"></div>
      <%= form.submit "Ajouter un livre à cet auteur", class: 'btn btn-primary col-xs-6' %>
      <div class="col-xs-3"></div>
    </div>

  </div>
<% end %>
